import ProjectCard from "../../components/ProjectCard";

const projectCardStory = {
  title: "Project Card",
  component: ProjectCard,
};

export default projectCardStory;

export const ProjectCardStory = () => {
  return <ProjectCard />;
};
